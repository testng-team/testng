/**
 * Tests for the values TestNG passes to a method. They cover {@code @Parameters},
 * {@code @Optional}, parameters declared in a suite file, and the TestNG objects injected into a
 * method signature.
 *
 * <p>Compare {@code org.testng.invocationcount}, which is about repeated invocation rather than the
 * values each invocation receives.
 *
 * <p>Classes handed to TestNG to produce that behavior live in {@code samples}, not here.
 */
package org.testng.parameters;
