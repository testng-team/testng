/**
 * Tests for {@code priority} on {@code @Test}. They check the order TestNG picks, and what happens
 * when priority meets parallel execution or a method dependency.
 *
 * <p>Classes handed to TestNG to produce that behaviour live in {@code samples}, not here.
 */
package org.testng.priority;
