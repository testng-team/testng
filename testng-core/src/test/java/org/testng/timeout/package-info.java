/**
 * Tests for the time-out TestNG applies to a method, a {@code <test>} or a suite. They check that a
 * time-out is enforced and not merely reported, and what a time-out inherited from {@code <suite>}
 * or {@code <test>} means under each parallel mode.
 *
 * <p>Classes handed to TestNG to produce that behavior live in {@code samples}, not here.
 */
package org.testng.timeout;
