/**
 * Tests for {@code IMethodInterceptor}. They check what happens when an interceptor reorders the
 * test methods, removes some of them, or runs beside another interceptor.
 *
 * <p>Classes handed to TestNG to produce that behavior live in {@code samples}, not here.
 */
package org.testng.methodinterceptors;
