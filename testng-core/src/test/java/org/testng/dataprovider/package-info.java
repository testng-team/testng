/**
 * Tests for what a {@code @DataProvider} that hands out no row does: the single skipped result it
 * produces, how often it is evaluated, and what it does to configuration methods, dependent methods
 * and the exit status.
 *
 * <p>Classes handed to TestNG to produce that behavior live in {@code samples}, not here.
 */
package org.testng.dataprovider;
